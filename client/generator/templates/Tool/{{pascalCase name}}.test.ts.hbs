import * as tools from './{{pascalCase name}}.tools'


describe('[{{pascalCase name}}] tools', () => {
  describe('[init{{pascalCase name}}] func', () => {
    it('should return null', () => {
      expect(tools.init{{pascalCase name}}()).toEqual(null)
    })
  })
})
