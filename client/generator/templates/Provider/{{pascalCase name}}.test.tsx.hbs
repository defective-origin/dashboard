// ---| tests |---
import { RenderResult, renderHook } from '@testing-library/react-hooks'

// ---| self |---
import { {{pascalCase name}}Options, use{{pascalCase name}} } from './{{pascalCase name}}.context'
import {{pascalCase name}}, { {{pascalCase name}}Props } from './{{pascalCase name}}.component'

/**
 * Context descriptions
 *
 * @example
 * const { result } = renderHook(() => useCounter())
 *
 * act(() => {
 *   result.current.increment()
 * })
 *
 * expect(result.current.count).toBe(1)
 */
describe('[{{pascalCase name}}] context', () => {
  let hookRef: RenderResult<{{pascalCase name}}Options>
  const wrapper = (props: {{pascalCase name}}Props) => <{{pascalCase name}} { ...props } />

  beforeEach(() => {
    hookRef = renderHook(use{{pascalCase name}}, { wrapper }).result
  })

  it('should return options', () => {
    expect(hookRef.current.current).toBe(null)
  })
})
