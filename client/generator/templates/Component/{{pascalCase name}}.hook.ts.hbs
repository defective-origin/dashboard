import { useState } from 'react'

export type Use{{pascalCase name}}Options = null

export type Use{{pascalCase name}}ReturnOptions = null

/**
 * Hook descriptions
 *
 * @example
 * const options = {{pascalCase name}}(conf)
 */
export const use{{pascalCase name}} = (options?: Use{{pascalCase name}}Options): Use{{pascalCase name}}ReturnOptions => {
  const [state] = useState<Use{{pascalCase name}}ReturnOptions>(null)

  console.log(options, state)

  return state
}

export default use{{pascalCase name}}
