/* eslint-disable no-restricted-imports */
import { field, params } from '../../../.storybook/tool'
import type { Meta, StoryObj } from '@storybook/react'
import { COLORS, SIZES } from 'theme'
import {{pascalCase name}}, { {{pascalCase name}}Props } from './{{pascalCase name}}.component'

const VARIANTS: {{pascalCase name}}Props['v'][] = ['success', 'info', 'warning', 'error']

const meta: Meta<typeof {{pascalCase name}}> = {
  component: {{pascalCase name}},
  title: 'Components/{{pascalCase name}}',
  tags: ['autodocs'],
  argTypes: {
    name: field.string(),
    className: field.string(),
    children: field.reactNode(),
  },
}

export default meta

type Story = StoryObj<typeof {{pascalCase name}}>

export const Demo: Story = {
  parameters: params('{{pascalCase name}}'),
  args: {
    name: 'Demo',
  },
}

export const Variants: Story = {
  parameters: params('View', VARIANTS),
  args: {
    name: 'Demo',
    v: 'success',
  },
}
