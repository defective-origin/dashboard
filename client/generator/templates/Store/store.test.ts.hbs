import * from './store.tools'

describe('[Store] tools', () => {
  describe('[action name] func', () => {
    it('should do something', () => {
      const mockValue = 0

      const result = doSomething(mockValue)

      expect(result).toEqual()
    })
  })
})
