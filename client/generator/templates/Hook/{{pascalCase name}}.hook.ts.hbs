import { useState } from 'react'

export type {{pascalCase name}}Options = null

export type {{pascalCase name}}ReturnOptions = null

/**
 * Hook descriptions
 *
 * @example
 * const options = {{camelCase name}}(conf)
 */
export const {{camelCase name}} = (options?: {{pascalCase name}}Options): {{pascalCase name}}ReturnOptions | null => {
  const [state] = useState<{{pascalCase name}}ReturnOptions | null>(null)

  console.log(options, state)

  return state
}

export default {{camelCase name}}
