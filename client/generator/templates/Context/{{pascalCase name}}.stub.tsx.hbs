import React from 'react'
import {
  {{pascalCase name}}Context,
  {{pascalCase name}}Options,
  DEFAULT_{{constantCase name}}_OPTIONS,
} from './{{pascalCase name}}.context'

export type {{pascalCase name}}StubProps = React.PropsWithChildren & Partial<{{pascalCase name}}Options>

export function {{pascalCase name}}Stub(props: {{pascalCase name}}StubProps): JSX.Element {
  const { children, ...otherProps } = props
  const combinedValue = { ...DEFAULT_{{constantCase name}}_OPTIONS, ...otherProps }

  return <{{pascalCase name}}Context.Provider value={combinedValue} children={children} />
}

export default {{pascalCase name}}Stub
