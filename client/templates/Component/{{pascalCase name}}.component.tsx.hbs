import React from 'react'

// ---| pages |---
// ---| screens |---
// ---| components |---
// ---| root |---

// ---| common |---
import { cn } from 'common/tools'

// ---| self |---
import css from './{{pascalCase name}}.module.scss'

export type {{pascalCase name}}Props = {
  className?: string
  children?: React.ReactNode
}

/**
 * Component description.
 *
 * How to use
 * @example
 * <{{pascalCase name}} />
 */
export function {{pascalCase name}}(props: {{pascalCase name}}Props): JSX.Element {
  const { children, className, ...otherProps } = props
  const _className = cn(css.{{pascalCase name}}, className)

  return <div className={_className} {...otherProps}>{children}</div>
}

export default {{pascalCase name}}
