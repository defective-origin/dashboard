import { useState } from 'react'

export type {{pascalCase name}}Options = {
  
}

export type {{pascalCase name}}ReturnOptions = {

}

/**
 * Hook descriptions
 *
 * @example
 * const options = {{pascalCase name}}(conf)
 */
export const {{camelCase name}} = (options?: {{pascalCase name}}Options): {{pascalCase name}}ReturnOptions => {
  const [state, setState] =  useState(null)

  return state
}

export default {{camelCase name}}
