{
  "name": "project",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev:client": "yarn workspace client run dev",
    "dev:server": "yarn workspace server run dev",
    "dev": "yarn workspaces foreach -pvi -A --include client --include server run dev",
    "build:client": "yarn workspace client run build",
    "build:server": "yarn workspace server run build",
    "build": "yarn workspaces foreach -pvi -A --include client --include server run build",
    "lint:client": "yarn workspace client run lint",
    "lint:server": "yarn workspace server run lint",
    "lint": "yarn workspaces foreach -pvi -A --include client --include server run lint",
    "test:client": "yarn workspace client run test",
    "test:server": "yarn workspace server run test",
    "test": "yarn workspaces foreach -pvi -A --include client --include server run test",
    "verify:client": "yarn workspace client run verify",
    "verify:server": "yarn workspace server run verify",
    "verify": "yarn workspaces foreach -pvi -A --include client --include server run verify",
    "packages:clean": "rimraf node_modules client/node_modules server/node_modules .yarn/cache yarn.lock",
    "packages:reinstall": "yarn packages:clean && yarn install",
    "ws": "yarn workspaces list --json"
  },
  "packageManager": "yarn@4.18.0",
  "devDependencies": {
    "rimraf": "6.1.3"
  }
}
