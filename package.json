{
  "name": "dashboard",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "workspaces": ["client", "server"],
  "scripts": {
    "dev": "yarn workspace dashboard-client run dev",
    "build": "yarn workspace dashboard-client run build",
    "verify": "yarn workspace dashboard-client run verify",
    "ws": "yarn workspaces info"
  }
}
